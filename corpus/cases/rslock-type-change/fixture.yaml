rowshape_fixture: "1"
meta:
  id: rslock-type-change
  engine: {name: postgres, version: "16"}
  privacy: standard
  profile: {mode: fast, escalated: []}
tables:
  public.events:
    rows: {value: 3000000, confidence: exact}
    columns:
      id: {type: bigint, nullable: false, unique: {value: true, confidence: exact, via: constraint}}
      amount: {type: integer, nullable: false}
    constraints: [{name: events_pkey, kind: primary_key, columns: [id]}]
