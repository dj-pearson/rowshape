rowshape_fixture: "1"
meta:
  id: rsdata-notnull-has-nulls
  engine: {name: postgres, version: "16"}
  privacy: standard
  profile: {mode: exact, escalated: [public.users.email]}
tables:
  public.users:
    rows: {value: 500000, confidence: exact}
    columns:
      id: {type: bigint, nullable: false, unique: {value: true, confidence: exact, via: constraint}}
      email: {type: text, nullable: true, null_fraction: {value: 0.02, confidence: exact, via: scan}}
    constraints: [{name: users_pkey, kind: primary_key, columns: [id]}]
