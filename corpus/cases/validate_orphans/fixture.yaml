rowshape_fixture: "1"
meta:
  id: corpus-validate-orphans
  engine: {name: postgres, version: "16"}
  privacy: standard
  profile: {mode: fast, escalated: []}
tables:
  public.users:
    rows: {value: 100000, confidence: exact}
    columns:
      id: {type: bigint, nullable: false, unique: {value: true, confidence: exact, via: constraint}, generated: identity}
    constraints: [{name: users_pkey, kind: primary_key, columns: [id]}]
  public.orders:
    rows: {value: 800000, confidence: exact}
    columns:
      id: {type: bigint, nullable: false, unique: {value: true, confidence: exact, via: constraint}, generated: identity}
      user_id: {type: bigint, nullable: false, distinct: 99000}
    constraints: [{name: orders_pkey, kind: primary_key, columns: [id]}]
    references:
      - column: user_id
        to: public.users.id
        on_delete: cascade
        fanout: {mean: 8, p50: 3, p95: 40, max: 900, confidence: measured}
        orphan_fraction: {value: 0.004, confidence: exact, via: scan}
